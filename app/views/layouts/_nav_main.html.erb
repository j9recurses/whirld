<div data-uk-sticky>
  <nav id='main-nav' class="wh-navbar uk-navbar uk-navbar-attached <%=className%> wh-caps uk-text-large">
    <a id='wh-logo' class="uk-navbar-brand" href="/" style="background-image:url('/assets/logo.png')"></a>

    <div class='uk-navbar-content'>
      <%= link_to("Create", maps_new_path ) %>
    </div>

    <ul class="uk-navbar-nav uk-navbar-flip">
        <li class="uk-parent" data-uk-dropdown="{mode:'click'}" aria-haspopup="true" aria-expanded="false">
            <a href=""> <%=current_user.login %> <i class="fa fa-caret-down"></i> </a>

            <div class="uk-dropdown uk-dropdown-navbar" data-uk-dropdown="{mode:'click'}">
                <ul class="uk-nav uk-nav-navbar">
                    <li><a href="#">Another item</a></li>
                    <li class="uk-nav-header">Header</li>
                    <li><a href="#">Item</a></li>
                    <li><a href="#">Another item</a></li>
                    <li class="uk-nav-divider"></li>
                    <li><a href="#"> <% if user_signed_in? %>
        Signed in as <%if user_signed_in? %>
                 <%= current_user.login %>
                <%else%>
                        <%= %>
                        <%end%>
       <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
    <% else %>
        <%= link_to 'Register', new_user_registration_path %> or <%= link_to 'Sign in', new_user_session_path %>
    <% end %></a></li>


                </ul>
            </div>
        </li>
    </ul>


        <% unless className == 'front' %>
          <div class="uk-navbar-content uk-navbar-flip">
            <%= render :partial => 'maps/search_pieces/top_search_bar' %>
          </div>
        <% end %>
  </nav>

  <% if className == 'search' %>
    <%= render :partial => "maps/search_pieces/filter_bar" %>
  <% end %>

</div>
