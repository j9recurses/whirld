
  <small class="text-muted"><%= a.created_at.strftime('%H:%M:%S %-d %B %Y') %></small><br/>
  <strong><%= activity.owner ? activity.owner.login : 'Guest' %></strong>
  <% if a.trackable %>
    added the project <%=a.trackable.name%>
  <% else %>
    added the project that is currently deleted.
  <% end %>

